# DI Specification Template
metadata:
  template_version: "1.0"
  llc_code: "DI"
  llc_name: "DOMAIN INTERFACE"
  tfa_layer: "SYSTEMS"
  domain: "[DOMAIN-CODE]"
  created: "[DATE]"
  author: "[AUTHOR]"

specification:
  id: "[DI-ID]"
  description: "[DESCRIPTION]"
  version: "[VERSION]"
  
  # DOMAIN INTERFACE specific configuration
  configuration:
    # Add DI-specific parameters here
    placeholder_param: "[VALUE]"
    
  # Standard TFA V2 metadata
  tfa_metadata:
    layer: "SYSTEMS"
    llc: "DI"
    quantum_enabled: false
    classical_bridge: false
    federation_capable: false

# Validation rules
validation:
  required_fields: ["id", "description", "version"]
  format_rules:
    id: "^[A-Z]{3}-DI-[0-9]{3}$"
    version: "^[0-9]+\.[0-9]+\.[0-9]+$"
