# SI Specification Template
metadata:
  template_version: "1.0"
  llc_code: "SI"
  llc_name: "SYSTEM INTEGRATION"
  tfa_layer: "SYSTEMS"
  domain: "[DOMAIN-CODE]"
  created: "[DATE]"
  author: "[AUTHOR]"

specification:
  id: "[SI-ID]"
  description: "[DESCRIPTION]"
  version: "[VERSION]"
  
  # SYSTEM INTEGRATION specific configuration
  configuration:
    # Add SI-specific parameters here
    placeholder_param: "[VALUE]"
    
  # Standard TFA V2 metadata
  tfa_metadata:
    layer: "SYSTEMS"
    llc: "SI"
    quantum_enabled: false
    classical_bridge: false
    federation_capable: false

# Validation rules
validation:
  required_fields: ["id", "description", "version"]
  format_rules:
    id: "^[A-Z]{3}-SI-[0-9]{3}$"
    version: "^[0-9]+\.[0-9]+\.[0-9]+$"
