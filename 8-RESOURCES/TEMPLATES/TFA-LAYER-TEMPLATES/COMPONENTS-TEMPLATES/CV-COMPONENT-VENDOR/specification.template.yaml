# CV Specification Template
metadata:
  template_version: "1.0"
  llc_code: "CV"
  llc_name: "COMPONENT VENDOR"
  tfa_layer: "COMPONENTS"
  domain: "[DOMAIN-CODE]"
  created: "[DATE]"
  author: "[AUTHOR]"

specification:
  id: "[CV-ID]"
  description: "[DESCRIPTION]"
  version: "[VERSION]"
  
  # COMPONENT VENDOR specific configuration
  configuration:
    # Add CV-specific parameters here
    placeholder_param: "[VALUE]"
    
  # Standard TFA V2 metadata
  tfa_metadata:
    layer: "COMPONENTS"
    llc: "CV"
    quantum_enabled: false
    classical_bridge: false
    federation_capable: false

# Validation rules
validation:
  required_fields: ["id", "description", "version"]
  format_rules:
    id: "^[A-Z]{3}-CV-[0-9]{3}$"
    version: "^[0-9]+\.[0-9]+\.[0-9]+$"
